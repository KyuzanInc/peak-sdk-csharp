# Build artifacts
bin/
obj/
out/
**/[Bb]in/
**/[Oo]bj/
**/[Oo]ut/

# NuGet
*.nupkg
*.snupkg
packages/*/lib/
**/*.nuget.dgspec.json
**/*.nuget.g.props
**/*.nuget.g.targets
project.lock.json
project.fragment.lock.json
artifacts/
/.artifacts/

# Node (OpenAPI codegen tooling only; pinned via package-lock.json + npm ci)
node_modules/

# IDE
.vs/
.vscode/
.idea/
*.user
*.suo
*.userosscache
*.sln.docstates
*.swp
*~

# OS
.DS_Store
Thumbs.db

# Test results
TestResults/
coverage*.xml
coverage*.json
coverage*.cobertura.xml
*.coverage
*.coveragexml

# Local artifacts
local/
tmp/

# Upstream sources (synced via scripts, not hand-edited)
# These dirs are committed; this section documents intent only.
# upstream-snapshots/

# Editor backup
*.orig
*.rej

# Secrets / local env (never commit)
.env
.env.local

# Stray tooling artifacts at repo root (codegen/tooling side effects)
/openapitools.json
/ns.dll

# Approval-test received output (only the .approved.txt is committed)
**/*.received.txt
