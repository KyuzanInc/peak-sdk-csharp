# peak-server OpenAPI snapshot

Tracks **`main`** of `KyuzanInc/peak` (not a release tag). Each resync grabs
`main`'s current HEAD and records the exact commit below, so the snapshot
stays a reproducible, pinned artifact while following main's latest.

| Tracks | Last synced commit | Snapshot date | Source path |
|---|---|---|---|
| `main` | `72ca08b3eade7117334ad481f6a1b29a6bee73a9` | 2026-05-30 | KyuzanInc/peak `apps/peak-public-docs/docs/api-references/public-api.yaml` |

> Re-sync with `scripts/sync-upstream.sh peak-server-openapi main`, then
> regenerate the client (`scripts/generate-public-api-client.sh`) and commit.
